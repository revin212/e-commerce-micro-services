# Fixture Payloads

This directory contains backend handoff payload samples copied from:

- `src/lib/api/mocks/fixtures/*.json`

Use these files as request/response examples while implementing backend endpoints.
